# Example environment file for docker-compose.yml
# Copy to .env (repo root) before running docker compose.
# NEVER commit your real .env

# Database
DB_PASSWORD=change_me_to_strong_password

# Required secrets (32+ chars recommended)
JWT_SECRET=CHANGE_ME_TO_A_STRONG_RANDOM_SECRET_AT_LEAST_32_CHARS
JWT_REFRESH_SECRET=CHANGE_ME_TO_A_DIFFERENT_STRONG_RANDOM_SECRET_AT_LEAST_32_CHARS
COOKIE_SECRET=CHANGE_ME_TO_ANOTHER_STRONG_RANDOM_SECRET_AT_LEAST_32_CHARS

# Optional
CORS_ORIGIN=http://localhost
RUN_DB_PUSH_ON_START=true
VITE_API_URL=http://localhost:5000
STORAGE_PROVIDER=local
MALWARE_SCAN_PROVIDER=none
MALWARE_SCAN_FAIL_MODE=fail_open
SENTRY_DSN=
