# Electrical Supplier Website - robots.txt
# Allow all search engines to crawl the site

User-agent: *
Allow: /

# Disallow admin panel
Disallow: /admin/
Disallow: /api/

# Sitemap location
Sitemap: http://localhost:5000/sitemap.xml

# NOTE:
# In production, the backend serves a domain-safe /robots.txt and /sitemap.xml dynamically.
# If you host the frontend separately, update the Sitemap URL to your real domain.
